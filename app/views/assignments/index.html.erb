assignments#index path

<%= @date_fix = remove_dashes(@date) %>

<% params[:format] = @date_fix %>

<%= link_to 'New Assignment', new_course_assignment_path(@course, @date), class: 'btn btn-success' %>