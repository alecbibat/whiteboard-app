<div class='span8 well'>

<h4> <%= @course.name %> </h4>
<p class="quiet"> <%= @course.department %> | <%= @course.number %> </p>

<%= form_for [@course, @post] do |f| %>

<div class="control-group">
<%= f.label :title %>
<%= f.text_field :title %>
</div>

<div class="control-group">
<%= f.label :description %>
<%= f.text_area :description %>
</div>

<div class="control-group">
<%= f.submit "Create Post", class: "btn btn-success" %>
</div>

<% end %>
</div>

<% @course.posts.reload.each do |post| %>
  <div class="span8 well">
    <h4><%= post.title %></h4>
    <p><%= post.description %></p>
    <p class="small quiet">created at <%= post.created_at %> by post.user.username</p>
  </div>
<% end %>